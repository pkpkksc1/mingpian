{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "🚀 Publish",
            "type": "shell",
            "command": "git add .; if ($?) { git commit -m 'Auto Update' }; if ($?) { git push origin main }",
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            }
        }
    ]
}