{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "🚀 Publish",
            "type": "shell",
            "command": "git add .; git commit -m 'Auto Update'; if ($LASTEXITCODE -eq 0) { git push origin main }",
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            }
        }
    ]
}